'use client'

// Tema dark aposentado em 03/06 — agendapro é light-only.
// Mantido como stub (renderiza nada) pra não quebrar os ~6 lugares que ainda
// importam/usam <ThemeToggle compact />. Remoção dos usos pode vir depois.
export default function ThemeToggle(_props: { compact?: boolean }) {
  return null
}
