# Diário — Sessão 2026-05-14 (manhã → noite, virou pro dia 15)

**Branch:** master
**Status:** ✅ tudo em produção (Vercel) — 5 migrations aplicadas no Supabase de PROD
**Commits da sessão:** **14** (de `ded6b4c` a `28800fd`)
**Linhas modificadas:** ~6.000 (insertions)
**Migrations rodadas em prod:** v42 · v43 · v44 · v45 · v46

---

## Diretrizes/decisões cravadas na sessão (vão pra memória)

1. **Caminho mais seguro pra notificação em massa = email** (Resend já configurado). Baileys multi-tenant tem risco real de colapso em escala (~50+ clientes) · WhatsApp API oficial fica pra quando justificar custo recorrente. Email cobre o caso enquanto valida produto.
2. **Toda feature universal usa pattern opt-in** (toggle por business · default OFF). Negócio existente não muda comportamento sem decisão explícita do dono.
3. **Migration aplicada ANTES de git push** quando código novo lê coluna nova. Se pushar antes · Vercel sobe e quebra prod (select de coluna inexistente).
4. **Uma decisão por vez ao cravar escopo de feature** — não bombardear o Eduardo com 10 perguntas simultâneas. Apresenta 1, ele crava, próxima.
5. **Feature universal não personaliza copy com nome de cliente real** — placeholders/exemplos genéricos · variação por categoria via `detectCategory()` quando precisar.

---

## O que foi feito (por área temática)

### 1. Sistema de transcrição local (whisper.cpp) — fora do AgendaPRO

Setup local pra transcrever áudios WhatsApp do Eduardo · pasta `C:/Users/Usuario/whisper/`:
- whisper.cpp BLAS x64 binary
- modelo ggml-large-v3-turbo.bin (1.6 GB)
- ffmpeg standalone
- wrapper `transcrever.ps1` PT-BR
- ~1x realtime no Ryzen 1600

Habilitou todo o resto da sessão (áudios da Leticia e Olímpio).

### 2. F1 · Import de dados (Leticia · Salão 365) — `ded6b4c` + 3 follow-ups

**Migration v42:** `customers.birthday/notes/import_source/import_external_id/imported_at` + `appointments.customer_id` (FK paralelo ao `client_id` legado v2). Indexes parciais.

**Backend src/lib/importers/:**
- `canonical.ts` · tipos CanonicalClient/Appointment + ImportReport
- `normalize.ts` · phone E.164 BR · email · data flex · parser CSV próprio · `parseXlsxBuffer` via SheetJS dynamic import (server-only)
- `salao365.ts` · connector com header detection tolerante a acento/case
- `run.ts` · UPSERT batch · dedupe external-id-then-phone/update/skip
- `index.ts` · registry + dispatch

**API:** `POST /api/import/preview` (dryRun · não grava) + `POST /api/import/commit` · auth + ownership via SSR client (RLS).

**UI universal:** tela única em `/admin/configuracoes?tab=importar` · botão de upload tracejado com IconUpload (SVG) · 4 steps inline · suporte CSV + XLSX.

**Hotfix v43:** constraint `subscriptions_provider_check` (v40) só aceitava `mercado_pago | asaas`. Estendido pra `cortesia` (bug do script grant-trial-viva-cacheada.sql que ninguém tinha testado).

**Teste end-to-end** no Império Barbershop com CSV de 15 linhas: 12 lidos · 12 inseridos · 3 skips corretos (sem nome, sem fone, telefone sem DDD) · preview 158ms · commit 418ms.

**Planilha real da Leticia recebida:** 209 clientes · 56 com aniversário · 1 telefone inválido · headers batem 100% com o connector. Aguardando ela subir (entrega via UI dela).

### 3. F1.5 · Modal cliente + booking aniversário — `5d04498` `4215c5c`

**Modal Detalhes do cliente** agora renderiza e edita `birthday` + `notes` + tag "Importado · Salão 365". API PATCH valida YYYY-MM-DD com round-trip via Date (rejeita 30/02 etc) · email regex · max name 200 · max notes 1000.

**Booking público** ganha campo opcional `Aniversário` com `max={hoje}` · grava em `customers.birthday` no INSERT (cliente novo) ou UPDATE (cliente existente sem birthday). Padrão "preenche se faltar" · nunca sobrescreve dado bom.

### 4. Cupom de aniversário (espelho Oi Sumido) — `010bc04`

Replicou padrão de campanha pra trigger de aniversário do mês:
- API existente `/api/admin/coupons/campaign` ganha branch `target='aniversariantes_mes'`
- Filtra `customers.birthday` cujo MÊS = mês atual
- Defaults mais generosos: 20% / 30 dias (vs 10% / 14 dias dos sumidos)
- `BIRTHDAY_TEMPLATES` em coupon-templates.ts (8 categorias)
- Nova `/admin/clientes/campanhas` com 3 abas (Sumidos · Aniversário · Avulso depois)
- Card no FocoDoDia "🎂 N aniversariantes esse mês"
- Reaproveitou 90% do ReativarSumidosView via componente novo enxuto

### 5. IDEIA 1 Olímpio · editar serviços do agendamento — `4a8b802` + `dbc2dfc`

**Caso real:** Olímpio marca cabelo+barba · cliente decide só barba · financeiro fica errado.

**API:** `PATCH /api/admin/appointments/[id]/services` aceita `serviceIds[]` · auth owner OU professional do appointment · valida pelo menos 1 serviço · valida services do mesmo business e ativos · recalcula end_time + total_price · pre-check de conflito (constraint v40 no_overlap como backup) · DELETE+INSERT em appointment_services + UPDATE denormalizados (`service_id` / `service_name` / `total_price` / `end_time`) · activity log.

**UI:** botão lápis SVG (24x24) ao lado do chip do serviço · só aparece em pending/confirmed/completed/no_show · `EditServicesModal` standalone com lazy-load · checkboxes · preview ao vivo (duração · horário fim · total) · mensagem clara em caso de conflito.

**Decisão cravada:** NÃO mexe em pontos (Olímpio ajusta manual como já faz hoje). Trigger `credit_points_on_confirm` (v15) continua intacto.

**Hardening pós-auditoria:** `revalidatePath('/admin')` + `'/profissional')` · `try/finally` + `mountedRef` · lápis 13px no botão 24x24 (era 11px).

### 6. IDEIA 3 Olímpio · cupom avulso pra divulgação — `d3ef697` + `2ede6b4`

**Migration v44:** `coupons.professional_id` (FK opcional) · `is_standalone` · `standalone_label` · tabela `coupon_redemptions` com UNIQUE `(coupon_id, customer_phone)` · RLS owner+public read.

**Backend:** `POST /api/admin/coupons/standalone` cria 1 cupom com `is_standalone=true · customer_id=null` · retorna code + share_url + whatsapp_text · `GET` lista ativos com contador de usos.

**APIs adaptadas:**
- `/api/coupons/lookup` aceita `?phone=` opcional pra checar `coupon_redemptions` se cupom é standalone
- `/api/coupons/use` distingue standalone (INSERT em `coupon_redemptions` · UNIQUE constraint pega 2º uso do mesmo telefone → 409) vs legacy (UPDATE `used_at`)

**UI:** `CupomAvulsoView` com form (nome opcional · profissional opcional · desconto · validade) · após gerar mostra código grande + copy link + 1-click WhatsApp · lista cupons ativos com contador. Cor da aba: **púrpura** (`#7C3AED`).

**Hardening pós-auditoria:**
- Universalização: trocou `"Promo do rapaz"` (do contexto Olímpio) por `"Inauguração · Indicação de cliente · Black Friday"` no placeholder
- Generalizou texto WhatsApp: `"Cupom pra cortar comigo"` → `"Cupom pra você no {businessName}"` (era específico de barbearia)
- `buildStandaloneWhatsappText` em coupon-templates.ts aceita `label` opcional como prefixo
- Cor do card de sucesso e badge de usos: alinhados com púrpura da aba

**Regra criada:** feature universal NÃO personaliza copy com nome de cliente real — virou memory [[universal-nao-personaliza-cliente]].

### 7. IDEIA 2 Olímpio · punição no-show + lembrete 3h — `f72bb7f` + `28800fd`

A mais sistêmica. 13 decisões cravadas com Eduardo (1 por vez) antes de codar.

**Decisões finais:**
1. Canal: **email** (caminho mais seguro · Resend já configurado · Baileys descartado por risco de colapso em escala)
2. Punição opt-in por business
3. Modo: Proporcional (default) OU Valor fixo (admin define)
4. Saldo clamp em 0 · nunca negativo
5. Aviso de transparência no booking público quando business tem punição
6. Aplicação **imediata** ao virar `no_show` (trigger SQL)
7. Lembrete **fixo 3h antes**
8. Lembrete **híbrido**: todos os agendamentos recebem · texto adapta se business tem punição
9. Email tem **link 1-click cancelar** via `generateCancelToken` (já existente)
10. Toggle na aba Fidelidade (não nova aba)
11. Vale pra todos agendamentos futuros (não só novos)
12. Descoberta: card FocoDoDia + points_transactions + tag inline (todos os 3)
13. Reverter: botão 1-click no card + ajuste manual como fallback

**Migrations:**
- v45 · 3 colunas em `businesses` (toggle/modo/fixed_points) + `reminded_3h` em appointments
- v46 · function `apply_no_show_penalty()` + trigger AFTER UPDATE de status. Espelho do trigger `credit_points_on_confirm` (v15)

**Backend:**
- `POST /api/admin/appointments/[id]/relevar` · reversão 1-click idempotente
- `GET /api/admin/appointments/[id]/penalty-info` · lazy-load pra card
- `/api/cron/reminder-3h` · cron a cada 15min · texto adaptativo · entry no vercel.json
- `sendReminderEmail` estendido pra type='3h' + appointmentId + noShowPenaltyPoints (todos opcionais · zero quebra retrocompat)

**UI:**
- `PunicaoNoShowCard` standalone com toggle + radio modo + input fixed · auto-save com debounce
- `FidelidadeTab` ganha o card · props propagadas via ConfiguracoesTabs
- `BookingFlow` ganha banner amarelo de transparência quando business tem punição (alinhado com padrão p-4 rounded-2xl pós-auditoria)
- `AppointmentCard` lazy-load penalty info quando status=no_show · tag "−Xpts" vermelha · botão "Reverter punição · devolver N pts" · tag "Punição revertida" cinza após relevar
- `FocoDoDia` ganha card cinza "N punições nas últimas 24h"

### 8. Auditorias filtradas

Sub-agente Explore rodou 4x na sessão (IDEIA 1 · IDEIA 3 · IDEIA 2 · hardening base). Cada auditoria gerou achados que filtramos contra feedbacks cravados antes de aplicar. Falsos alarmes recorrentes:
- `revalidatePath` falsamente apontado quando `cache: 'no-store'` já bypassa
- "Cor hardcoded" quando é padrão estabelecido do design system
- "Botão sem disabled" quando JÁ TEM disabled (auditor não leu direito)
- "Padding diferente" quando contextos são diferentes (modal com footer fixo vs sem)

Pattern aplicado: `tom direto sem frescura` + `filtrar recomendações de outros agentes` (memórias cravadas).

---

## Pendências reais (não bloqueantes)

1. **Leticia subir o CSV do Salão 365** (209 clientes) pra validar import em prod
2. **Avisar o Olímpio** que as 3 ideias dele estão no ar pra ele testar e ligar toggle de punição se quiser
3. **Backlog técnico** identificado em auditoria:
   - UNIQUE constraint em `(appointment_id, reason)` pra blindar reversão dupla teórica
   - N+1 em `getFocoDoDia` pode virar single JOIN se aparecer hotspot
   - `generateCancelToken` sem expiração (pré-existente · não introduzido nesta sessão)

---

## Aprendizados operacionais

**Supabase SQL Editor não limpa entre runs** — colou ALTER novo, ficou com pedaço de SQL antigo no buffer e deu syntax error. Solução: abrir **aba SQL nova** (botão `+`) sempre que mudar de bloco. Acumulou ~30min de tentativas frustradas até descobrir.

**Migration grande em 6 blocos pequenos > 1 bloco grande** — pra evitar exatamente o problema acima · cada bloco numa aba.

**Audit pattern consolidado:**
1. Sub-agent Explore com prompt estruturado (PARTE A bugs · PARTE B visual)
2. Filtrar achados contra memórias cravadas
3. Aplicar só o que sobrar
4. Memory feedback ajuda blindar contra alarme falso recorrente

---

## Estado final do AgendaPRO em prod

- **5 migrations aplicadas** (v42 v43 v44 v45 v46)
- **3 clientes ativos** (Olímpio · Erlane · Leticia)
- **15 commits do dia** no ar
- **6 features novas** ativas (todas opt-in ou aditivas · zero quebra retrocompat)
- **TypeScript** + **next build**: limpos em todas as iterações
- **agendapro.net.br/admin/login**: HTTP 200 confirmado pós-último push

Bom dia, próxima sessão. Sistema 100% liso.
